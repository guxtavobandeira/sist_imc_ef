<?php
include_once('conexao.php');

?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        #quadrado {
            display: none; /* Inicialmente, o quadrado está oculto */
            width: 400px;
            min-height: 400px; /* Altura mínima para acomodar o conteúdo */
            background-color: red; /* Cor de fundo do quadrado */
            position: fixed; /* Posicionamento fixo para centralizar na tela */
            top: 50%; /* Posiciona o topo do quadrado no meio vertical da tela */
            left: 50%; /* Posiciona a esquerda do quadrado no meio horizontal da tela */
            transform: translate(-50%, -50%); /* Move o quadrado de volta metade da sua própria largura e altura, centralizando-o */
            text-align: center; /* Centraliza o texto horizontalmente */
            color: white; /* Cor do texto */
            font-size: 24px; /* Tamanho do texto */
            padding: 20px; /* Adiciona um espaçamento interno ao quadrado */
        }
        #quadrado button {
            margin: 10px; /* Margem entre os botões */
        }
        h1 {
            display: inline; /* Torna os títulos <h1> elementos em linha */
            margin: 0 10px; /* Adiciona um espaçamento horizontal entre os títulos */
        }
    </style>
    <script>
        function exibir_quadrado(event){
            event.preventDefault(); // Evita o comportamento padrão do link
            var quadrado = document.getElementById('quadrado');
            quadrado.style.display = 'block'; // Exibe o quadrado
        }
    </script>
</head>
<body>
    <a href="#" onclick='exibir_quadrado(event)' >clique para exibir</a>
    <div id="quadrado">Você realmente deseja excluir o aluno?
        <h1>Nome do aluno:</h1>
        <h1>Curso:</h1>
        <h1>Série:</h1>
        <br>
        <br>
        <button>confirmar</button>
        <button>cancelar</button>
    </div>
</body>
</html>

